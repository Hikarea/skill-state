diff --git a/hermes_cli/main.py b/hermes_cli/main.py
index 8281cbdd63..1a04c278ca 100644
--- a/hermes_cli/main.py
+++ b/hermes_cli/main.py
@@ -210,6 +210,7 @@ def _run_and_exit_oneshot(
     toolsets: object = None,
     skills: object = None,
     usage_file: object = None,
+    resume: object = None,
 ) -> None:
     try:
         from hermes_cli.oneshot import run_oneshot
@@ -221,6 +222,7 @@ def _run_and_exit_oneshot(
             toolsets=toolsets,
             skills=skills,
             usage_file=usage_file,
+            resume=resume,
         )
     except KeyboardInterrupt:
         rc = 130
@@ -13101,6 +13103,7 @@ def _try_fast_chat_launch() -> bool:
             toolsets=getattr(args, "toolsets", None),
             skills=getattr(args, "skills", None),
             usage_file=getattr(args, "usage_file", None),
+            resume=getattr(args, "resume", None),
         )
 
     if (args.resume or args.continue_last) and args.command is None:
@@ -13159,6 +13162,7 @@ def _try_termux_fast_cli_launch() -> bool:
             toolsets=getattr(args, "toolsets", None),
             skills=getattr(args, "skills", None),
             usage_file=getattr(args, "usage_file", None),
+            resume=getattr(args, "resume", None),
         )
 
     if (args.resume or args.continue_last) and args.command is None:
@@ -15158,6 +15162,7 @@ def main():
             toolsets=getattr(args, "toolsets", None),
             skills=getattr(args, "skills", None),
             usage_file=getattr(args, "usage_file", None),
+            resume=getattr(args, "resume", None),
         )
 
     # Handle top-level --resume / --continue as shortcut to chat
diff --git a/hermes_cli/oneshot.py b/hermes_cli/oneshot.py
index e2778d67d7..3c3d96a839 100644
--- a/hermes_cli/oneshot.py
+++ b/hermes_cli/oneshot.py
@@ -206,6 +206,7 @@ def run_oneshot(
     toolsets: object = None,
     skills: object = None,
     usage_file: Optional[str] = None,
+    resume: Optional[str] = None,
 ) -> int:
     """Execute a single prompt and print only the final content block.
 
@@ -221,6 +222,8 @@ def run_oneshot(
             cost, token counts, model, api_calls) is written there after the
             run — even when the run fails — so pipelines can account for
             spend per invocation.
+        resume: Optional existing session ID whose transcript should precede
+            this prompt.
 
     Returns the exit code.  The caller owns process termination.
     """
@@ -283,6 +286,7 @@ def run_oneshot(
                     toolsets=explicit_toolsets,
                     use_config_toolsets=use_config_toolsets,
                     skills=skills,
+                    resume=resume,
                 )
             except BaseException as exc:  # noqa: BLE001
                 # Capture anything that escapes the agent (including OSError
@@ -354,6 +358,23 @@ def _create_session_db_for_oneshot():
         return None
 
 
+def _load_resumed_session(session_db, session_id: str) -> tuple[str, list[dict]]:
+    """Resolve and safely load an existing session for one-shot continuation."""
+    if session_db is None:
+        raise RuntimeError("Session database not available; cannot resume")
+    if not session_db.get_session(session_id):
+        raise ValueError(f"Session not found: {session_id}")
+
+    resolved_id = session_db.resolve_resume_session_id(session_id) or session_id
+    session_db.assert_resume_safe(resolved_id, tip_only=True)
+    history = session_db.get_messages_as_conversation(
+        resolved_id, repair_alternation=True
+    )
+    history = [message for message in history if message.get("role") != "session_meta"]
+    session_db.reopen_session(resolved_id)
+    return resolved_id, history
+
+
 def _run_agent(
     prompt: str,
     model: Optional[str] = None,
@@ -361,6 +382,7 @@ def _run_agent(
     toolsets: object = None,
     use_config_toolsets: bool = True,
     skills: object = None,
+    resume: Optional[str] = None,
 ) -> tuple[str, dict]:
     """Build an AIAgent exactly like a normal CLI chat turn would, then
     run a single conversation.  Returns ``(final_response, run_result)``."""
@@ -484,6 +506,11 @@ def _run_agent(
     # os._exit and skips finalizers, so an un-closed connection here would leak.
     agent = None
     try:
+        session_id = None
+        conversation_history = None
+        if resume:
+            session_id, conversation_history = _load_resumed_session(session_db, resume)
+
         # Read the effective fallback chain from profile config so oneshot
         # workers honour the same merge semantics as interactive CLI and
         # gateway sessions.
@@ -500,6 +527,7 @@ def _run_agent(
             quiet_mode=True,
             platform="cli",
             session_db=session_db,
+            session_id=session_id,
             credential_pool=runtime.get("credential_pool"),
             fallback_model=_fb or None,
             ephemeral_system_prompt=skills_prompt,
@@ -523,7 +551,9 @@ def _run_agent(
         agent.stream_delta_callback = None
         agent.tool_gen_callback = None
 
-        result = agent.run_conversation(prompt)
+        result = agent.run_conversation(
+            prompt, conversation_history=conversation_history
+        )
         return (result.get("final_response") or "", result)
     finally:
         # Ordering deliberately mirrors gateway/run.py:_cleanup_agent_resources,
diff --git a/toolsets.py b/toolsets.py
index a40d803538..dc8fd6d576 100644
--- a/toolsets.py
+++ b/toolsets.py
@@ -101,6 +101,11 @@ _HERMES_WEBHOOK_SAFE_TOOLS = [
 # Core toolset definitions
 # These can include individual tools or reference other toolsets
 TOOLSETS = {
+    "state-only": {
+        "description": "No model tools; actions are executed by the state runtime allowlist",
+        "tools": [],
+        "includes": [],
+    },
     # Basic toolsets - individual tool categories
     "web": {
         "description": "Web research and content extraction tools",
