# Repository workflow

- Commit and push authorized changes directly to `main`. Do not create pull requests unless the user explicitly requests one.
- Preserve concurrent changes; never force-push over another commit.
- Keep benchmark claims tied to their implementation and dataset. Distinguish provider tokens, serialized bytes, local context-management time, and end-to-end model time.
